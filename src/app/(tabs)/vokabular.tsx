import { EmptyState, Screen } from '@/components/ui';

export default function VokabularScreen() {
  return (
    <Screen>
      <EmptyState title="Vokabular" hint="Liste (decks) i pojmovi dolaze uskoro." />
    </Screen>
  );
}
