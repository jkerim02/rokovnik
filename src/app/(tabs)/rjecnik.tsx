import { EmptyState, Screen } from '@/components/ui';

export default function RjecnikScreen() {
  return (
    <Screen>
      <EmptyState title="Rječnik" hint="Pretraga i ručno dodavanje dolaze uskoro." />
    </Screen>
  );
}
