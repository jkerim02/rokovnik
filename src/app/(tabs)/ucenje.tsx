import { EmptyState, Screen } from '@/components/ui';

export default function UcenjeScreen() {
  return (
    <Screen>
      <EmptyState title="Učenje" hint="Kvizovi (vokabular i citati) dolaze uskoro." />
    </Screen>
  );
}
